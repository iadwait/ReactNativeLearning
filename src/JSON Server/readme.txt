1. Create Folder JSON Server
2. Navigate to that folder and install json server globally "npm i -g json-server"
3. Check if JSON Server is installed "json-server"
4. Create users.json create dummy response and execue "npx json-server users.json"